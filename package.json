{
  "name": "webrtc-cam-bridge",
  "private": true,
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "npm -w server run dev & npm -w web run dev",
    "dev:server": "npm -w server run dev",
    "dev:web": "npm -w web run dev",
    "lint": "echo \"(add lint later)\"",
    "build": "echo \"(build both later)\""
  }
}